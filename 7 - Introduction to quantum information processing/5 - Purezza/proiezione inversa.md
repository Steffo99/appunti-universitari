[[Operazione]] che restitusce l'[[ampiezza di uno stato quantistico|ampiezza]] dello [[stato base passivo]] di un dato [[qbit]].

$$
\Huge
\dot{\mathbf{n}}
$$

Corrisponde al [[ket-bra]] tra lo [[stato base passivo]] e sè stesso.
$$
\Large
\dot{\mathbf{n}} = \ket{0}\bra{0}
$$
